"""Proton dose calculation submodules.

Package marker — required so ``opentps.core.processing.doseCalculation.protons.MCsquare``
resolves and ``radiarch.services.machine_model`` can find the vendored
BDL and Scanner data files on disk.
"""
